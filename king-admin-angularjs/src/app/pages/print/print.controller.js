(function() {
    'use strict';

    angular.module('KingAdmin.pages.print')
        .controller('PrintControlller', PrintControlller);

    /** @ngInject */
    function PrintControlller($scope, $rootScope, $window, UserService) {

        var kt = this;

    }
})();